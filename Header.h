#pragma once
#include <iostream>

// Insertion Algorithm:
// This takes each element that is unsorted, then finally sorts it to the correct place 
// by removing the wrong number and placing it where it belongs in the sort. It does this
// until all of them are sorted. 
void InsertionArray(int arr[], int size);

//Prints the array.
void printArray(int arr[], int size);